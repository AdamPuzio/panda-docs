---
sidebar_position: 1
---
import Tabs from '@theme/Tabs'
import TabItem from '@theme/TabItem'

# Commands

## Setup

### Setup Using `panda`

The `panda` library contains `Command`, along with a lot of other entities and functionality.

```bash npm2yarn
npm install panda
```

### Setup Using `@panda/command`

If you _just_ want to use `Command`, you can install it directly from `@panda/command`:

```bash npm2yarn
npm install @panda/command
```

## Usage

<Tabs>
<TabItem value='cjs' label='CommonJS'>

```js
const { Command } = require('panda')
```

</TabItem>
<TabItem value='esm' label='ESM'>

```js
import { Command } from 'panda'
```

</TabItem>
<TabItem value='ts' label='Typescript'>

```typescript
import { Command } from 'panda'
```

</TabItem>
</Tabs>


```js title='Test Code Block' showLineNumbers
// highlight-next-line
import { Command } from 'panda'

const x = 0
// highlight-start
const y = 1
const z = 2
// highlight-end
```

```bash npm2yarn
npm install @docusaurus/remark-plugin-npm2yarn
```
