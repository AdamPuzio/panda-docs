---
title: scaffoldDir
description: Directory where scaffold files are stored
tags: [Scaffold, Scaffold Properties]
---

# `scaffoldDir`

The `scaffoldDir` property is a string that specifies the directory where scaffold files are stored. This will be the base directory for any `source` paths specified in the scaffold.
