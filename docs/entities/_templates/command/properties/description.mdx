# `description`

A short, friendly text description of the {props.entity} for use in the {props.entity}'s help section.

The description is used in 2 places
- within the `--help` menu of the {props.entity}
- within the `--help` menu of any {props.entity}s that include it as a subcommand