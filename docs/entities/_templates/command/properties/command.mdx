# `command`

Provides the default command to be called when imported, usually as a subcommand.

If not explicitly set, defaults to value of `name`. Can be overriden when importing via subcommands by specifying a new command as the key.