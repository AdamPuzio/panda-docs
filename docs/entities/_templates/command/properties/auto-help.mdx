# `autoHelp`

The `autoHelp` property is a boolean value that toggles the automated `--help` flag for a command.

When set to false, the `--help` flag will not be automatically added to the command. This is useful when you want to handle the `--help` flag manually.
